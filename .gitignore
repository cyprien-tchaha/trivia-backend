.env
venv/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Spartan AI config (contains API keys)
.spartan/ai.env

# Redis snapshot, written into the working directory by a locally-started
# redis-server (docker compose keeps its own in a volume).
dump.rdb
